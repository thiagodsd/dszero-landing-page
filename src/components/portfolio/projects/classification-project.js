import React from 'react';
import metadata from './classification-project-metadata';

const ClassificationProject = () => (
    <div>
        <h1>{metadata.title}</h1>
        {/* Project content */}
    </div>
);

export default ClassificationProject;