import React from 'react';
import metadata from './clustering-project-metadata';

const ClusteringProject = () => (
    <div>
        <h1>{metadata.title}</h1>
        {/* Project content */}
    </div>
);

export default ClusteringProject;